**/__pycache__/
*.py[cod]
*.egg-info/
build/
.pytest_cache/
logs/
outputs/
motions/videos/
# Git cannot re-include a path whose parent directory is excluded, so the
# directory is not excluded -- its contents are, with the scripts exempted.
figures/*
!figures/*.py
