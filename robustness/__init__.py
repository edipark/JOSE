"""Sensor-degradation experiments: how each method fails as its inputs get worse.

Evaluation-only, except for the one training entry point that gives JOSE
encoder-noise randomization. Nothing here is imported by the ablation studies,
so none of it is fingerprinted and none of it can disturb a logged result.
"""
